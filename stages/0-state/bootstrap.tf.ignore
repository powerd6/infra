# This file is used only in case of bootstrapping or disaster.
# See the "Special considerations" section in the README to understand how to do this.

terraform {
  required_version = ">= 1.6.0"
  required_providers {
    elephantsql = {
      source  = "elephantsql/elephantsql"
      version = "0.0.1"
    }
  }
}